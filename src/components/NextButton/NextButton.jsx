import React from 'react';
import classes from './NextButton.module.scss';

const NextButton = () => {
    return (
        <p className={classes.NextButton}>NextButton</p>
    )
}

export default NextButton